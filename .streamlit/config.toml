# App theme (safe to commit — no secrets here).
[theme]
base = "dark"
primaryColor = "#7C4DFF"
backgroundColor = "#0E0E14"
secondaryBackgroundColor = "#1A1A24"
textColor = "#EDEDF2"
font = "sans serif"
